[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "parser-core"
version = "0.1.0"
description = "Deep and generic parsers for Codebase Intelligence"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["src/parser_core"]
