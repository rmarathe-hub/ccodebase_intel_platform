import { PageShell } from "../components/PageShell";

export function RepositoryOverviewPage() {
  return (
    <PageShell
      title="Repository overview"
      description="Language totals, deep vs generic support, and snapshot metadata for an imported repository."
    />
  );
}
