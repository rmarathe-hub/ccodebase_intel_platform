import { PageShell } from "../components/PageShell";

export function FilesPage() {
  return (
    <PageShell
      title="Files"
      description="Browse the repository file tree and open sources in the Monaco viewer with citation highlighting."
    />
  );
}
