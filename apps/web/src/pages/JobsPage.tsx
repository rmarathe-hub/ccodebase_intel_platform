import { PageShell } from "../components/PageShell";

export function JobsPage() {
  return (
    <PageShell
      title="Jobs"
      description="Track indexing progress through clone, discover, parse, relationship, chunk, embed, and validate stages."
    />
  );
}
