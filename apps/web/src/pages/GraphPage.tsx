import { PageShell } from "../components/PageShell";

export function GraphPage() {
  return (
    <PageShell
      title="Graph"
      description="Explore module, package, directory, caller/callee, and inheritance relationships."
    />
  );
}
