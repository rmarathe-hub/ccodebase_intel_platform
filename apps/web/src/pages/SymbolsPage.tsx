import { PageShell } from "../components/PageShell";

export function SymbolsPage() {
  return (
    <PageShell
      title="Symbols"
      description="Browse verified deep symbols and clearly labeled generic heuristic sections."
    />
  );
}
